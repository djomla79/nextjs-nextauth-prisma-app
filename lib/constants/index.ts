import { BASE_URL, SMTP_USER, SMTP_PASSWORD, JWT_SECRET_KEY } from './config';
import { activationUserStatus } from './user';

export {
  BASE_URL,
  SMTP_USER,
  SMTP_PASSWORD,
  JWT_SECRET_KEY,
  activationUserStatus,
};
